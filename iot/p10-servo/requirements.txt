# --- Core Framework ---
flask==3.1.2

# --- MySQL Connector berbasis Python murni (tanpa libmysql native) ---
mysql-connector-python==8.4.0

# --- Opsional (debugging, keamanan, REST API) ---
python-dotenv==1.0.1 # untuk simpan config .env
flask-cors==4.0.1 # kalau nanti akses API dari front-end (mis. React / HTML)
requests==2.32.5 # kalau nanti ESP32 kirim data via HTTP POST
Werkzeug==3.1.3 # dependency Flask, untuk keamanan session cookies